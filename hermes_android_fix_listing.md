# GUMROAD LISTING — "Fix Your Broken Hermes on Android ($0 VM Escape)"
# Sellable mini-course born from the real diagnosis this session. ETHICS: 100% legal,
# accurate. We do NOT claim to "repair" Android — we route AROUND it via a free/cheap
# Linux VM. No underhanded claims. State the real cost. Reputation-first.

=================================================================
PRODUCT TITLE
=================================================================
Fix Your Broken Hermes on Android — The $0 VM Escape (Browser + Web Search Working)

=================================================================
TAGLINE (subtitle)
=================================================================
They shipped you a tool with a dead browser on Android. Here's the verified fix that
actually works — no node thrashing, no guessing. Run Hermes on a free Linux VM and get
a working browser + live web search in 10 minutes.

=================================================================
PRICE
=================================================================
$27 standalone mini-course.
BUNDLE: free bonus inside the $197 "AI Influencer + Tax-Smart Starter" tier
  (teach the method AND the tool fix together).

=================================================================
WHAT IT IS (plain, honest)
=================================================================
Hermes Agent's browser and web_search tools crash on Android/Termux. Not your fault.
Not a setting. It's a device-level wall (Android's linker vs Node's V8). We hit four
dead ends proving it — and found the one fix that works: run Hermes on a real Linux
VM (free tier). This mini-course gives you the exact, copy-paste steps.

=================================================================
WHAT YOU GET
=================================================================
1. The Diagnosis (plain English) — why the browser dies, with the 4 dead ends we hit
   (node V8 crash, no chromium pkg, no pip packages, firecrawl Rust fail). So you
   STOP wasting time.
2. The $0 Fix — GitHub Codespaces route (browser-backed, drivable from your phone).
3. The Always-On Fix — Oracle Free Tier route (free VM, SSH from phone).
4. Copy-paste command blocks (verified, not theory).
5. How to bring your existing build over (git/scp).
6. The 2-line test that PROVES your browser works again.

=================================================================
WHO IT'S FOR
=================================================================
- Hermes users on Android/Termux with a dead browser/web_search.
- Anyone who wants live web research + a real browser inside their agent.
- Builders who'd rather spend 10 min on the fix than 10 hours thrashing node versions.

=================================================================
HONEST NOTES (no hiding)
=================================================================
- The fix uses a cloud VM. GitHub Codespaces free tier = $0 (auto-pauses when idle).
  Oracle Free Tier = $0 always-on (needs a card on file for ID, not charged).
  If you want a paid always-on box, Hetzner/DigitalOcean run ~$5/mo. We state all of it.
- We did NOT "repair" Android. We routed around it with a proper Linux environment.
  Accurate framing — and it's the reason the fix actually holds.
- This is a setup guide, not a magic patch. You run real commands on a real VM.

=================================================================
GUARANTEE
=================================================================
If you follow the steps and your Hermes browser + web_search don't work on the VM,
email me — I'll help you through it or refund. No sneaky fine print.

=================================================================
UPSELL / CONNECTION
=================================================================
This pairs with our "AI Influencer + Tax-Smart Starter" course: build a real
work-from-home business AND the tooling to run it. Same ethics, same no-BS style.

=================================================================
KEYWORDS / TAGS
=================================================================
hermes agent, android, termux, broken browser, node v8 crash, web search not working,
linux vm, codespaces, oracle free tier, ai agent setup, fix

=================================================================
LEGAL / ETHICS FOOTER
=================================================================
All methods are legal and above-board. We do not circumvent terms of service, do not
misrepresent the fix, and do not sell "hacks." The diagnosis is session-verified.
Use at your own discretion; you run the commands on infrastructure you control.
